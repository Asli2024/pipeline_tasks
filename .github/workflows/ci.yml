name: CICD Workflow

on:
    push:
        branches:
            - main
    pull_request:
        branches:
            - main

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            - name: Testing workflow
              run: echo "This is a test workflow for CI/CD pipeline tasks."
